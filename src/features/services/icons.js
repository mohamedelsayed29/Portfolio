import { BrainCircuit, Bug, Compass, Layers, Server, Smartphone } from 'lucide-react'

/** Explicit map — keeps icons in the bundle without dynamic-import indirection. */
export const SERVICE_ICONS = {
  Layers,
  Server,
  Smartphone,
  BrainCircuit,
  Bug,
  Compass,
}
